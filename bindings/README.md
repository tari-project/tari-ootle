# Tari Ootle Typescript bindings

## Overview

This repository contains the TypeScript bindings for the Tari Ootle library. The bindings are generated from rust types
using [ts_rs](https://docs.rs/ts-rs/latest/ts_rs/).
