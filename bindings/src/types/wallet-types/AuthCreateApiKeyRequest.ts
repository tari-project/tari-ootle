// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.

/**
 * Admin → daemon: mint a new long-lived API key with the supplied scopes.
 *
 * `permissions` is the textual form `JrpcPermissions::from_str` accepts
 * (e.g. `["AccountInfo", "TransactionGet"]`). `confirm_admin` must be
 * `true` iff the list contains `Admin` — deliberate speed-bump so the UI
 * can surface an explicit warning before issuing a fully-privileged key.
 */
export type AuthCreateApiKeyRequest = {
  name: string;
  permissions: Array<string>;
  confirm_admin: boolean;
};
