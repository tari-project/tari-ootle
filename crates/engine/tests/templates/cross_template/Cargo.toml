[workspace]
[package]
name = "cross_template"
version = "0.1.0"
edition = "2024"

[dependencies]
tari_template_lib = { path = "../../../../template_lib" }
tari_bor = { path = "../../../../tari_bor" }

[lib]
crate-type = ["cdylib"]
