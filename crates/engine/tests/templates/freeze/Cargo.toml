[workspace]
[package]
name = "recall"
version = "0.1.0"
edition = "2024"


[dependencies]
tari_template_lib = { path = "../../../../template_lib", default-features = false, features = ["macro"] }

[lib]
crate-type = ["cdylib", "lib"]
